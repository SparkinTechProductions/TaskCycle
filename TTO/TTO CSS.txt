
#checkbox-list {
    max-height: calc(100vh - 375px); /* Prevent overlap with header and footer */
    overflow-y: auto; /* Enable scrolling within the list */
    /*width: 100%; /* Full width of the parent container */
    max-width: 600px; /* Limit the maximum width of the checklist */
    margin: auto; /* Center the checklist horizontally */
    display: flex;
    flex-direction: column; /* Stack child items vertically */
    padding: 7px;
    border: 1px solid #ccc; /* Optional: Add a border for clarity */
    border-radius: 8px; /* Rounded corners for style */
    min-width: 200px; /* Minimum width for the checklist */
    box-sizing: border-box; /* Include padding in the dimensions */
}
#checkbox-list {
    position: relative; /* Make it stay fixed on the screen */
    top: 38%; /* Position the checklist vertically in the center */
    left: 50%; /* Center horizontally */
    transform: translate(-50%, -50%); /* Adjust to align center perfectly */
    padding: 10px; /* Add some padding inside the container */
    overflow-y: auto; /* Allow scrolling within the checklist for long tasks */
    
}




/* Hide content when in landscape mode */
@media (orientation: landscape) and (max-width: 1024px) {
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f8f9fa;
        overflow: hidden;
    }
    
    .landscape-warning {
        display: block;
        text-align: center;
        font-size: 1.2rem;
        color: #333;
        z-index: 1001;
    }
    
    #app {
        display: none; /* Hide the app's content */
    }
}

@media (orientation: portrait) and (max-width: 1024px) {
    .landscape-warning {
        display: none; /* Hide the warning in portrait */
    }

    #app {
        display: block; /* Show the app's content */
    }
}


* For portrait mode on tablets and phones */
@media (orientation: portrait) and (max-width: 1024px) {
    .landscape-warning {
        display: none; /* Hide the warning in portrait */
    }

    #app {
        display: block; /* Show the app's content */
    }
}


.title-section {
    margin-bottom: 30px; /* Add extra space below the logo */
}

/* Desktop behavior (no warning) */
@media (min-width: 1025px) {
    .landscape-warning {
        display: none; /* Never show the warning on larger devices */
    }

    #app {
        display: block; /* Always show the app content */
    }
}


/*
#add-button {
    font-size: 90px;
    background: black;     
    color: white;
    border: 2px solid white;
    border-radius: 50%;      
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    display: inline-flex;   
    justify-content: center;
    align-items: center; /* Ensures text is centered vertically */
    width: 100px; /* Set width and height to ensure it's a circle */
    height: 100px; /* Match height to width */
    padding: 0; /* Reset padding to avoid affecting the shape */
    box-sizing: border-box; /* Ensures border doesn't increase the size */
}
*/

/* Add these styles to your styles.css file */

/*
#chart-button {
    position: fixed;
    bottom: 20px;
    right: 20px;
    z-index: 100; 
    /* Ensures the button is above other elements */
    /* Add more styling as needed (size, colors, etc.) 
}
*/
/*
#pie-chart-container canvas {
    width: 100%;
    height: 300px;
}
*/

/* Shared circular style for both notes and chart buttons */
#notes-button,
#chart-button {
  position: fixed;
  bottom: 20px;         /* anchored at the bottom */
  width: 57px;          /* fixed width */
  height: 57px;         /* fixed height -> perfect circle */
  font-size: 24px;      /* adjust as needed for icon/text size */
  color: #ffffff;       /* white text/icon */
  background-color: rgb(78, 77, 77); /* black circle fill */
  border: 3px solid white;
  /* or a faint glow */
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.3);  
  border-radius: 50%;   /* makes it circular */
  cursor: pointer;
  z-index: 20;
  display: flex;        /* center text/icon horizontally & vertically */
  align-items: center;
  justify-content: center;
  transition: transform 0.2s, box-shadow 0.2s;
}

/* Position them separately: notes-button on the left, chart-button on the right */
#notes-button {
  left: 25px;
}

/* If you want the chart button hidden initially, you can keep display: none here */
#chart-button {
  right: 25px;
  display: none; /* remove this if you want it visible on page load */
}

#notes-button:hover,
#chart-button:hover {
  transform: translateY(-2px);
  /* Slightly lighten the background */
  background-color: rgb(100, 100, 100);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
}


#notes-button:active,
#chart-button:active {
  transform: translateY(0);
  box-shadow: none;
  background-color: #333; /* slightly lighter black when pressed */
}



.triangle {
    position: absolute;
    width: 400px;
    height: 300px;
    /* Use the same repeating-linear-gradient as your ".lines" class */
    background: repeating-linear-gradient(
      45deg,
      rgba(150, 150, 150, 0.548),
      rgba(150, 150, 150, 0.582) 5px,
      transparent 5px,
      transparent 10px
    );
  
    /* Clip to a triangle shape */
    clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
  
    /* Match your desired position, rotation, and opacity */
    
    top: 00%;
    left: -220px;
    transform: rotate(-110deg);
    opacity: 1;
    
  }
  